# Release 发布规范

本仓库从 `v1.0.0` 起采用语义化版本：`MAJOR.MINOR.PATCH`。

## 必须创建 Release 的情况

以下更新合并到主分支后必须发布 GitHub Release：

- 新增可直接使用的功能；
- 修改模型类别或默认权重；
- 推理结果格式变化；
- 修复影响识别/计数正确性的 Bug；
- 新增重要部署方式；
- 不兼容的配置或接口变化。

纯文档拼写修正可以只提交，不强制 Release。

## 发布步骤

1. 完成代码和测试；
2. 更新 `CHANGELOG.md`；
3. 确定版本号；
4. 提交主分支；
5. 创建对应 Tag，例如 `v1.1.0`；
6. 创建 GitHub Release；
7. Release Notes 至少包括：新增、修复、模型/数据变化、兼容性、升级方式；
8. 如有可分发模型或安装包，在不违反第三方许可的前提下附到 Release Assets。

## 版本规则

- `PATCH`：Bug 修复，例如 `v1.0.1`；
- `MINOR`：向后兼容的新功能，例如 `v1.1.0`；
- `MAJOR`：不兼容变化，例如 `v2.0.0`。

## 模型版本

模型发生变化时，Release Notes 必须写明：

- 模型来源/训练数据来源；
- 类别列表是否变化；
- 输入分辨率；
- 关键评估指标；
- 是否需要用户重新下载权重。
